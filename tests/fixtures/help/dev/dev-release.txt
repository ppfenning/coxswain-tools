usage: cox dev release [-h] [--manifest MANIFEST] [--dry-run] [--root ROOT] [--checkout NAME=PATH]
                       [--umbrella UMBRELLA] [--allow-doc-drift REASON]
                       version

positional arguments:
  version

options:
  -h, --help            show this help message and exit
  --manifest MANIFEST
  --dry-run
  --root ROOT
  --checkout NAME=PATH
  --umbrella UMBRELLA
  --allow-doc-drift REASON
                        proceed despite a standing release-check drift, naming why
