usage: cox dev [-h] ...

moved: run `uv run --frozen python -m devtools <command> ...` from the coxswain checkout (from
0.15.0)

positional arguments:
  rest

options:
  -h, --help  show this help message and exit
