usage: cox stats gates [-h] [--db DB] [--since SINCE] [--json]

options:
  -h, --help     show this help message and exit
  --db DB
  --since SINCE  keep only rows dated on or after DATE (YYYY-MM-DD)
  --json
