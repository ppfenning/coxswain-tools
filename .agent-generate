# Regenerated by `cox runs land` in the pull-request worktree (see README "Maintainers").
# COX_WORKTREE is that worktree; COX_UMBRELLA is the routing profile's umbrella_dir.
cd "$COX_UMBRELLA" && PYTHONPATH="$COX_WORKTREE" uv run --frozen python -m devtools commands render --target readme --readme "$COX_WORKTREE/README.md"
